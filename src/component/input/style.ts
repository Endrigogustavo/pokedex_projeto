import { StyleSheet } from 'react-native';

export const Styles = StyleSheet.create({
  input: {
    width: '100%',
    height: 52,
    borderWidth: 1,
    borderColor: '#ccc',
    borderRadius: 8,
    paddingHorizontal: 16,
    fontSize: 16,
    color: '#000',
    marginBottom: 12,
  }
});
